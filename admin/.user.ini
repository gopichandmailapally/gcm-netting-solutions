; GCM Admin — PHP-FPM compatible limits (replaces php_value in .htaccess)
max_execution_time = 300
memory_limit = 256M
post_max_size = 32M

; OPcache: always revalidate to avoid stale cached scripts
opcache.validate_timestamps = 1
opcache.revalidate_freq = 0
